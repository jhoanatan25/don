$( function() {
	$( "#datepicker" ).datepicker();
    $(".calendar").on("click",function(){
    	$("#datepicker").datepicker("show");
    });
 } );

 function menu(op)
 {
 	console.log(op);
 	$('.option').removeClass('active')

 	if(op!="none")
 		$(op).addClass('active')
 }
